<UserControl x:Class="Eikones.Views.DestinationColumnView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Eikones.ViewModels"
             AllowDrop="True"
             DragOver="DestinationColumn_OnDragOver"
             Drop="DestinationColumn_OnDrop">
    <Border BorderBrush="#E0E0E0" BorderThickness="1,0,0,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Margin="8,8,8,4"
                       FontSize="11"
                       Foreground="#666666"
                       Text="{Binding DestinationList.StatusMessage}"
                       TextTrimming="CharacterEllipsis"/>

            <Button Grid.Row="1"
                    Margin="8,0,8,4"
                    Content="Select Folder"
                    Padding="8,2"
                    HorizontalAlignment="Stretch"
                    Command="{Binding SelectDestinationFolderCommand}"/>

            <Grid Grid.Row="2" Margin="8,0,8,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0"
                         Margin="0,0,8,0"
                         Padding="4,2"
                         Text="{Binding DestinationList.NewFolderName, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Grid.Column="1"
                        Content="New Folder"
                        Padding="8,2"
                        Command="{Binding CreateNewDestinationFolderCommand}"/>
            </Grid>

            <ListBox x:Name="DestinationList"
                     Grid.Row="3"
                     ItemsSource="{Binding DestinationList.Files}"
                     SelectedItem="{Binding DestinationList.SelectedFile, Mode=TwoWay}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling"
                     VirtualizingPanel.CacheLengthUnit="Page"
                     VirtualizingPanel.CacheLength="1,1">
                <ListBox.Resources>
                    <ContextMenu x:Key="DestinationFileContextMenu">
                        <MenuItem Header="Delete"
                                  Command="{Binding DataContext.DestinationList.DeleteFileCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                  CommandParameter="{Binding}"/>
                        <MenuItem Header="Restore to original folder"
                                  Command="{Binding DataContext.DestinationList.RestoreFileCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                  CommandParameter="{Binding}"/>
                    </ContextMenu>
                </ListBox.Resources>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="ContextMenu" Value="{StaticResource DestinationFileContextMenu}"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:ImageItemViewModel}">
                        <TextBlock Text="{Binding FileName}"
                                   FontSize="12"
                                   Margin="8,4"
                                   TextTrimming="CharacterEllipsis"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <TextBlock Grid.Row="3"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Foreground="#999999"
                       Text="No images in destination folder"
                       Visibility="{Binding DestinationList.Files.Count, Converter={StaticResource ZeroToVisibilityConverter}}"/>
        </Grid>
    </Border>
</UserControl>
