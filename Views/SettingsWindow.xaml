<Window x:Class="Eikones.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Height="220"
        Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="Source folder:"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="8,0" Text="{Binding SourceFolderPath, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True"/>
        <Button Grid.Row="0" Grid.Column="2" Content="Browse..." Command="{Binding BrowseSourceCommand}" Padding="12,4"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,12,0,0" VerticalAlignment="Center" Text="Destination folder:"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="8,12,8,0" Text="{Binding DestinationFolderPath, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True"/>
        <Button Grid.Row="1" Grid.Column="2" Margin="0,12,0,0" Content="Browse..." Command="{Binding BrowseDestinationCommand}" Padding="12,4"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Width="90" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="Save" Width="90" IsDefault="True" Click="Save_OnClick"/>
        </StackPanel>
    </Grid>
</Window>
